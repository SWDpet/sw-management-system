# [기획서] 인프라 구성도 — 성능 개선 + 필터 UX 개선

- **작성팀**: 기획팀
- **작성일**: 2026-04-18
- **선행**: 커밋 `af2a1b0`(C 본체) → `a0941cd`(C 설명) → (A 인프라 구성도 구현 — 커밋 예정)
- **선행 문서**: [docs/product-specs/system-graph-infra.md](./system-graph-infra.md) v2
- **상태**: v2 (codex 재검토 대기) — 1차 검토 4건 반영

### 개정 이력
| 버전 | 변경 |
|------|------|
| v1 | 초안 |
| v2 | codex 검토 반영: ①FR-7/NFR-5 에 v2 호환성 회귀 매트릭스 추가 ②FR-3/FR-6 SW 숨김 상태 정의 보강(빈 상태/카운트/새로고침 초기값) ③NFR-3 현실 기준선 재정립 방침 명시 ④FR-2 단일 선택 고정 근거 + 다중 선택 승격 조건 명시 |
| v3 | 2026-04-19 감사 P2 3-4/3-5 조치: (1) 렌더러 방향 전환 반영 — vis-network hierarchical 실험 후 **텍스트 트리(CLI tree 스타일) 기본 + 조직도(≤80 노드) 토글** 로 확정. (2) 사용자 피드백 반영으로 **유형 필터 → 시스템명(sys_nm_en) 필터** 로 전환 (실구현 준수). (3) FR-4 배치 DataSet 업데이트 → HTML `innerHTML` 1회 replace 로 대체. |

---

## 1. 배경 / 목표

### 배경
A 탭(인프라 구성도) 구현 후 실데이터로 시험한 결과, **초기 로드가 매우 느림**. 기획서 v2 NFR-1 은 "인프라 100개 기준 500ms" 였으나 실제 DB 데이터는 훨씬 큼:

| 레벨 | 수량 |
|------|------|
| 지자체 (city\|dist) | 124 |
| 시스템 | 204 |
| 서버 | 331 |
| 소프트웨어 | 983 |
| **총 노드** | **1,642** |
| **총 엣지** | **1,518** |

**부하 지점**: vis-network `barnesHut` 물리 시뮬레이션이 1600+ 노드에서 stabilize 200회 반복하며 브라우저 수 초간 블로킹.

### 목표
- **초기 렌더 ≤ 1.5 초** 로 개선 (현재는 수 초~수십 초로 추정).
- 화면에서 **SW(983 개) 는 기본 숨김** — 필요시 토글.
- **필터 UI 통일성 강화**: 인프라 유형 선택을 드롭다운 방식으로 변경 (시·도/시·군·구와 일관성).
- 구조적 변경이지만 DB/API 계약 변화 없음(Frontend 만 수정).

---

## 2. 사용자 시나리오

1. 관리자가 A 탭 진입 → **즉시** (1.5초 이내) 트리 표시. 물리 시뮬레이션 없이 **텍스트 트리**(CLI tree `├── │ └──` 스타일) 로 렌더.
2. 기본 상태에서는 **소프트웨어 노드 보이지 않음**. 필터 바에 **"소프트웨어 표시" 체크박스**가 있어 사용자가 켜면 SW 레벨이 추가 표시됨.
3. **시스템명(sys_nm_en) 드롭다운** 하나로 유형을 좁힘 (전체 / 실제 DB 의 sys_nm_en 고유값들) — 시·도/시·군·구 드롭다운과 통일.
4. 필터 변경 시 **즉시 반영** — 트리 HTML 을 한 번에 재생성해 `innerHTML` 로 replace (1600+ 노드도 수 ms).
5. 필요 시 **조직도(org chart) 토글** 활성화 — 가시 노드 ≤ 80 일 때만 CSS flex 기반 조직도로 전환.
6. 새로고침 버튼 유지, 데이터 DB 재조회.

---

## 3. 화면 / UI 흐름

### 3-1. 필터 바 변경 (Before → After)

**Before (v2)**:
```
[시·도▼] [시·군·구▼] ●☑UPIS ●☑KRAS ●☑기타 [새로고침]
```

**After (v3 — 실구현)**:
```
[시·도▼] [시·군·구▼] [시스템: 전체▼] ☐ 소프트웨어 표시 [◉ 트리 / ○ 조직도] [새로고침]
```

- 시스템명 드롭다운: `전체` + DB 에서 조회한 고유 `sys_nm_en` 목록. 기본 "전체".
- 소프트웨어 체크박스: 기본 OFF. 켜면 software 레벨 노드·엣지 표시.
- 뷰 토글: 기본 "트리". 가시 노드 ≤ 80 일 때만 "조직도" 활성화.

### 3-2. 렌더러 — 텍스트 트리 + 조직도 토글

- **텍스트 트리(기본)**: 순수 HTML/CSS. 계층 indent 와 CLI 스타일 가지 문자
  (`├── │ └──`) 를 `<pre>` 내부에 렌더. vis-network 미사용.
- 트리 생성 → `container.innerHTML = treeHtml` 단일 replace. 1,642 노드도 브라우저 파싱 후 수 ms 내 표시.
- **조직도(토글)**: CSS flex 기반 상-하 조직도. 가시 노드 ≤ 80 구간에서만 표시.
  초과 시 비활성 상태로 회색 처리.

### 3-3. 노드 스타일
- 지자체 / 시스템 / 서버 / 소프트웨어 각각 CSS class 로 색상·굵기 구분.
- 색상은 기존 INFRA_LEVEL_COLORS 팔레트 계승.

### 3-4. 빈 상태 / 에러 상태
- 기존 FR-12 유지(필터로 모든 노드 숨겨지면 "필터 조건에 맞는 인프라가 없습니다").

---

## 4. 기능 요건 (FR)

| ID | 내용 |
|----|------|
| FR-1 | **텍스트 트리 렌더러를 기본 뷰로 사용** — 순수 HTML/CSS `<pre>` 기반, CLI tree 스타일 가지 문자. vis-network hierarchical 은 도입하지 않는다 (실험 후 제외). 조직도 뷰를 **토글 옵션**으로 제공하되 가시 노드 ≤ 80 구간에서만 활성. |
| FR-2 | **시스템명(sys_nm_en) 드롭다운**으로 필터. 옵션: `전체` + DB 실제 고유 sys_nm_en. 기본값 `전체`. **단일 선택 고정.** 다중 선택 요구 누적 시 Phase 2 재검토 — 승격 조건은 v2 와 동일. |
| FR-3 | "소프트웨어 표시" **체크박스** 신설. 기본 체크 해제(OFF). 체크 시 software 레벨 노드 · 서버→SW 엣지 모두 표시. **페이지 새로고침·재진입 시 OFF 로 복귀** (세션 저장 안 함 — 이번 범위). |
| FR-4 | `onInfraFilter()` 는 가시 노드 집합을 재계산한 뒤 트리 HTML 문자열을 **1회 생성 → `container.innerHTML` 한 번에 replace**. DataSet 배치 업데이트 방식은 더 이상 해당 없음 (vis-network 미사용). |
| FR-5 | 시스템명 드롭다운 값이 **비어있거나 "전체"** 이면 시스템명 필터를 적용하지 않음 (모든 sys_nm_en 통과). |
| FR-6 | "소프트웨어 표시" 해제 상태에서는, 설사 다른 필터 조건을 통과해도 software 레벨 노드와 그 엣지는 모두 숨김. SW 를 숨긴다는 이유만으로 "필터 조건에 맞는 인프라 없음" 빈 상태 메시지를 띄우지 않는다 — city/infra/server 중 하나라도 visible 이면 트리 정상 표시. 빈 상태 메시지는 모든 레벨의 **전체 노드가 0건인 경우에만** 출력. |
| FR-6-COUNT | 필터 바 또는 상세 패널 상단에 **현재 표시 중인 레벨별 카운트**를 간단히 노출 (예: `지자체 12 · 시스템 48 · 서버 77 · SW 숨김`). SW OFF 상태에서는 SW 카운트를 "숨김" 으로 표기. |
| FR-7 | 상세 패널 / 호버 툴팁 / 도메인 상속(v2 FR-2) / 민감 필드 제외(v2 FR-3) / 권한 ADMIN(v2 NFR-5) 모두 **기존 동작 유지**. 이 기획은 표현·성능만 수정. 아래 회귀 매트릭스 통과 기준 명시. |

### FR-7 회귀 매트릭스 (v2 호환성 pass 기준)

| 항목 | 검증 시나리오 | Pass 기준 |
|------|--------------|-----------|
| ADMIN 권한 | 비로그인/비-ADMIN 으로 `/api/infra` 호출 | HTTP 302 또는 403 |
| 민감 필드 배제 | `/api/infra` 응답 바디 grep | `acc_pw/sw_acc_pw/acc_id/sw_acc_id/mac_addr` + camelCase 변형 **0 hits** |
| 도메인 상속 | 서버/SW 노드에 시스템명 필터 적용 | 조상 시스템의 sys_nm_en 기반으로 필터 동작 |
| 상세 패널 | 서버 클릭 | IP/OS/모델/시리얼/CPU 만 (비밀번호·MAC 없음) |
| 호버 툴팁 | 시스템 노드 호버 | 시스템명 + sys_nm_en 표시 |
| FR-1-MERGE (C탭) | C 탭 진입 | 여전히 33 노드 정상 (A 개선이 C 에 영향 없음) |
| FR-8 | 서버 API 응답 구조(`nodes/edges/domain/attrs`) **변경 없음**. 백엔드 미수정. |

---

## 5. 비기능 요건 (NFR)

| ID | 내용 |
|----|------|
| NFR-1 | **초기 렌더 시간 ≤ 1.5 s** — A 탭 진입 후 노드가 화면에 배치 완료까지. (기준: 현 DB 규모 1,600+ 노드, Chrome 기준. DevTools Performance 탭으로 측정) |
| NFR-2 | 필터 조작 후 반영 시간 ≤ 300ms (배치 업데이트 덕분에). |
| NFR-3 | API 응답 시간 500ms 기준은 v2 기획서 NFR-1 **유지** — 백엔드 변경 없음. **응답 크기 기준은 재정립**: v2 의 200KB 는 "인프라 100개 기준" 이었으나 현 실데이터(시스템 204 / 서버 331 / SW 983)에서는 초과 가능. 개발계획서 단계에서 **실측 후 기준선 갱신** (예상 350~500KB). 초과 시 Phase 2 분할 로딩 검토. |
| NFR-4 | 민감 필드 제외(v2 NFR-3), 권한 ADMIN(v2 NFR-5) **유지**. |
| NFR-5 | 기능 회귀 없음 — 기존 T1~T14 중 필터 로직·shape 관련 항목은 본 개선에 맞게 갱신하되 **상세 패널/민감 필드/권한 테스트는 그대로 통과**해야 함. 구체적 Pass 기준은 FR-7 회귀 매트릭스 참조. 개발계획서 테스트 표에 매트릭스 각 항목에 대응하는 T# 반드시 포함. |

---

## 6. DB팀 자문 결과

**해당 없음** — 백엔드/DB 변경 0.
**DB팀 의견: 승인 (영향 없음).**

---

## 7. 의사결정 / 우려사항

### 7-1. 렌더러 선정 — ✅ 최종 확정 (v3)
- **선택**: 텍스트 트리(기본) + 조직도(≤80 노드 토글).
- 경위: v2 단계에서 vis-network `layout.hierarchical` 을 실험했으나
  ① 노드 겹침·라벨 가독성·브라우저 stabilize 비용이 여전히 존재,
  ② 사용자가 원한 "한눈에 보이는 트리 구조" 가 force/hierarchical 모두에서
  기대에 못 미침.
- 결론: 1,600+ 노드에서 가장 가볍고 확정적인 순수 HTML 텍스트 트리 +
  소규모 구간용 조직도 토글로 확정.

### 7-2. SW 기본 숨김 — ✅ 확정
- 근거: SW 983개 = 전체의 60%. 대다수 사용자는 서버 레벨까지 확인이 일반적.

### 7-3. 시스템명 드롭다운 vs 유형 체크박스 — ✅ 확정 (v3)
- 사용자 피드백: 유형(infra_type) 3종 구분보다 sys_nm_en 세분화가 실무에 유용.
- 실구현: DB 에서 distinct `sys_nm_en` 을 조회해 옵션으로 노출.
- 다중 선택은 v2 FR-2 승격 조건 유지.

### 7-4. 노드 드래그 — 해당 없음
- 텍스트 트리/조직도 모두 정적 HTML — 드래그 개념 없음. 재배치 요구 시 향후 Phase 에서 재검토.

### 7-5. 기존 v2 기획서와의 관계
- 본 기획서는 v2 의 일부 FR(레이아웃·필터 UI) 를 대체하지만, v2 전체를 폐기하지 않음. 구조·권한·민감정보 등 핵심 요구사항은 v2 그대로.

---

## 8. 영향 범위

| 계층 | 파일 | 유형 |
|------|------|------|
| Frontend (수정) | `src/main/resources/templates/admin-system-graph.html` | 필터 바 HTML 수정, JS 로직(`renderInfraGraph`, `onInfraFilter`) 개편, 새 `onInfraShowSwChange` 추가 |
| Backend | **없음** | API 변경 없음 |
| DTO | **없음** | 기존 InfraGraphDTO 그대로 |
| DB / Entity / Repository / Service | **없음** | — |

**수정 1개 파일. 백엔드/DB 전혀 변경 없음.**

---

## 9. 리스크 평가

| 리스크 | 수준 | 완화책 |
|--------|------|--------|
| 텍스트 트리 가독성 — 긴 라벨에서 줄바꿈 | 낮음 | 라벨 최대 길이 제한 + 툴팁으로 전체 노출 |
| SW 기본 숨김을 모르는 사용자가 "SW 안 보임" 으로 착각 | 낮음 | 체크박스 라벨 `소프트웨어 표시`, 빈 상태 아님에도 부재감 주지 않도록 UI 명확히 |
| 드롭다운 단일 선택으로 동시 보기 희귀 수요 불충족 | 낮음 | 기본 "전체" 옵션 제공. 필요시 별도 Phase 에서 multi-select |
| innerHTML 대량 replace 시 스크롤 위치 초기화 | 낮음 | 렌더 전 스크롤 오프셋 저장 → 후 복원 |
| 조직도 토글이 노드 초과 시 비활성 → 사용자가 원인 파악 어려움 | 낮음 | 비활성 시 툴팁으로 "노드 ≤ 80 필요" 안내 |

---

## 10. 승인 요청

본 기획서에 대한 codex 검토 및 사용자 최종승인을 요청합니다.
