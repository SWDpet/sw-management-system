-- ============================================================
-- V006: 기능사 → 기술자 명칭 변경
-- 이미 DB에 '기능사'로 입력된 데이터가 있을 경우 실행
-- ============================================================

UPDATE qt_wage_rate SET grade_name = REPLACE(grade_name, '기능사', '기술자')
WHERE grade_name LIKE '%기능사%';
