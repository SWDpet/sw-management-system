{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "swmanager",
      "runtimeExecutable": "cmd",
      "runtimeArgs": ["/c", "echo Spring Boot already running on port 8080 via server-restart.sh"],
      "port": 8080,
      "autoPort": true
    }
  ]
}
